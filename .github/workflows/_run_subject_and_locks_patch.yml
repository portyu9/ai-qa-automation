name: Temporary promotion adversarial finalizer

on:
  push:
    branches:
      - automation/autonomous-dependency-promotion
    paths:
      - .github/workflows/_run_subject_and_locks_patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Checkout authority branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: automation/autonomous-dependency-promotion
          fetch-depth: 1
      - name: Apply final promotion adversarial checks
        run: python .github/scripts/_bind_dispatch_subject.py
      - name: Commit final promotion adversarial checks
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add .github/scripts/dependency_promotion.py scripts/verify_fork_cloud_authority.py
          git diff --cached --check
          git commit -m 'test: harden dependency promotion policy'
          git push origin HEAD:automation/autonomous-dependency-promotion
