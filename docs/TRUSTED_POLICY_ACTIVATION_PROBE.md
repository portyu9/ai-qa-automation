# Trusted Actions Policy Activation Probe

This disposable document exists only to create a controlled pull-request subject for validating the active repository Actions Policy and the trusted `repository_dispatch` path.

It carries no runtime authority and must not be treated as production behavior or merge-authority evidence by itself.
